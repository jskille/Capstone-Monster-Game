#pragma strict

function Start () {

}

function Update () {
		
		// Sun Movement
		this.transform.position.x = this.transform.position.x + Time.deltaTime + .008;
		 //this.transform.position.y = Time.deltaTime + .008;
			
	
}